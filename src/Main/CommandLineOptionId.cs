namespace Main;

internal enum CommandLineOptionId
{
    Undefined = 0,
    Help,
    Version,
    ListAliases,
    RunAsAdmin
}

internal enum ExecutionMode
{
    Undefined = 0,
    Normal,
    Admin
}
